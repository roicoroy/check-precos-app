import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'check.precos.br',
  appName: 'bar-scanner-txt',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
