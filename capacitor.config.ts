import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'check.precos.br',
  appName: 'Check precos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
